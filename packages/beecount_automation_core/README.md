# beecount_automation_core

Pure-Dart candidate lifecycle, deduplication, posting decisions, mapping, rules, and retention logic. It consumes API DTOs and never performs platform I/O or writes BeeCount transactions directly.
